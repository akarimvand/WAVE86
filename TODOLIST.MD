# 📋 نقشه راه و پیشرفت فازبه‌فاز سیستم مدیریت باشگاه (PHP & MySQL TODOLIST)

این لیست شامل بازنویسی کامل، تمیز، فرم‌به‌فرم و ۱۰۰٪ دقیق تمام بخش‌های بک‌اند، دیتابیس و رابط کاربری وب سیستم مدیریت باشگاه بر پایه **Pure PHP & MySQL** است.

---

## 🟢 فاز ۰: زیرساخت، دیتابیس جامع و هسته مرکزی (Phase 0: Core Architecture & Database) [تکمیل شد]
- [x] **۰.۱ ساختار پروژه:** ایجاد ساختار فایل‌ها و پوشه‌های ماژولار در `/php_version`.
- [x] **۰.۲ دیتابیس جامع (MySQL Schema):** ایجاد فایل `schema.sql` شامل ۱۵ جدول اصلی با کلیدهای خارجی، ایندکس‌ها، انکودینگ `utf8mb4_unicode_ci` و اکانت مدیر پیش‌فرض.
- [x] **۰.۳ کلاس مدیریت PDO و امنیت:** پیاده‌سازی `Database.php` با پشتیبانی از تراکنش‌های مالی (Transactions)، کوئری‌های آماده (Prepared Statements) و جلوگیری از SQL Injection.
- [x] **۰.۴ هسته روتر و میدل‌ورها:** تنظیم `config.php` و `api/index.php` برای مدیریت CORS، Session، پردازش ورودی‌های JSON و پاسخ‌های استاندارد.
- [x] **۰.۵ توابع کمکی (Helpers):** پیاده‌سازی متدهای تولید شناسه یکتا (UUID)، اعتبارسنجی ورودی‌ها و فرمت‌بندی پاسخ‌ها.

---

## 🟢 فاز ۱: سیستم احراز هویت و مدیریت دسترسی‌ها (Phase 1: Authentication & RBAC) [تکمیل شد]
- [x] **۱.۱ ورود با کد ملی:** پیاده‌سازی سرویس ورود، اعتبارسنجی کد ملی و بررسی رمز عبور هش‌شده (`password_verify`).
- [x] **۱.۲ مدیریت نشست و خروج:** اندپاینت‌های `/api/auth/me` و `/api/auth/logout` با مدیریت ایمن Session.
- [x] **۱.۳ بازیابی و ثبت‌نام سریع مدیر:** سرویس خودکار ایجاد مدیر اولیه سیستم در صورت خالی بودن دیتابیس (`AuthController::ensureDefaultAdmin`).
- [x] **۱.۴ فرم ورود وب PHP:** پیاده‌سازی صفحه ورود اختصاصی `login.php`.

---

## 🟢 فاز ۲: مدیریت کاربران، پیش‌ثبت‌نام و سیستم اولیا/فرزندان (Phase 2: Users & Parents) [تکمیل شد]
- [x] **۲.۱ مدیریت کامل کاربران:** دریافت لیست با فیلتر نقش و جستجو (`UserController::list`).
- [x] **۲.۲ سرویس کاربر:** افزودن، ویرایش، مسدودسازی و مدیریت حساب کاربران (`UserController::save`).
- [x] **۲.۳ سیستم پیش‌ثبت‌نام ۴ مرحله‌ای:** پیاده‌سازی کامل فرم پیش‌ثبت‌نام آنلاین ۴ مرحله‌ای در `pre_register.php` (هویتی، تماس، تجهیزات/سلامت، اضطراری/ولی).
- [x] **۲.۴ پنل تایید پیش‌ثبت‌نام:** صفحه بررسی، تایید و رد درخواست‌های پیش‌ثبت‌نام در `pre_register_admin.php`.
- [x] **۲.۵ فرم آنلاین مدیریت اعضا و اولیا:** صفحه جامع `users.php` با فرم مودال افزودن/ویرایش کاربر (Member360) و فرم اتصال اولیا به فرزندان.

---

## 🟢 فاز ۳: سانس‌ها، دوره‌های آموزشی و ثبت‌نام (Phase 3: Courses & Enrollments) [تکمیل شد]
- [x] **۳.۱ مدیریت سانس‌ها:** تعریف سانس با تعیین مربی، روزهای برگزاری، ساعات، ظرفیت و شهریه (`CourseController::save`).
- [x] **۳.۲ ثبت‌نام ورزشکاران:** محاسبه خودکار تاریخ انقضا، تسویه شهریه و ظرفیت‌سنجی (`CourseController::enroll`).
- [x] **۳.۳ فرم مدیریت سانس‌ها:** صفحه `courses.php` با فرم مودال تعریف/ویرایش سانس و فرم مودال ثبت‌نام ورزشکار در دوره.

---

## 🟢 فاز ۴: سیستم حضور و غیاب (Phase 4: Attendance System) [تکمیل شد]
- [x] **۴.۱ ثبت حضور و غیاب:** ثبت گروهی توسط مربی (حاضر، غایب، غایب موجه، تاخیر) (`AttendanceController::saveBatch`).
- [x] **۴.۲ فرم حضور و غیاب:** صفحه `attendance.php` با انتخاب سانس، تاریخ جلسه، لیست ورزشکاران، کلیدهای رادیویی وضعیت و ذخیره گروهی.

---

## 🟢 فاز ۵: امور مالی، درآمدها، بدهکاران و بستانکاران (Phase 5: Financial Ledger) [تکمیل شد]
- [x] **۵.۱ تراکنش‌ها:** ثبت درآمدهای شهریه و بوفه و هزینه‌های باشگاه با متدهای نقدی، پوز و درگاه (`FinanceController::save`).
- [x] **۵.۲ دفتر بدهکاران و بستانکاران:** مدیریت طلب‌ها و بدهی‌های مربیان و اعضا (`FinanceController::getDebtors`).
- [x] **۵.۳ فرم دفتر مالی:** صفحه `finance.php` با کارت‌های خلاصه مالی و فرم مودال ثبت تراکنش درآمد/هزینه.

---

## 🟢 فاز ۶: بوفه، انبارداری و صدور فاکتور (Phase 6: Shop & Inventory) [تکمیل شد]
- [x] **۶.۱ بوفه:** تعریف کالاها، قیمت خرید/فروش، موجودی انبار و حد هشدار (`ShopController::saveProduct`).
- [x] **۶.۲ صدور فاکتور:** فروش اقلام با کسر خودکار از انبار و ثبت درآمد مالی (`ShopController::createInvoice`).
- [x] **۶.۳ فرم بوفه و صندوق POS:** صفحه `shop.php` با لیست کالاها، فرم افزودن محصول و صندوق فروش آنلاین با کسر خودکار موجودی.

---

## 🟢 فاز ۷: بیمه ورزشی، اعلانات و پشتیبانی (Phase 7: Insurance & Support) [تکمیل شد]
- [x] **۷.۱ بیمه ورزشی:** فرم و صفحه `insurance.php` جهت بارگذاری کارت بیمه و پنل تایید/رد ادمین.
- [x] **۷.۲ تیکتینگ پشتیبانی:** فرم و صفحه `support.php` جهت ثبت تیکت جدید و فرم پاسخ‌دهی مدیر سیستم.
- [x] **۷.۳ سامانه پیامک و بله:** فرم و صفحه `sms.php` جهت ارسال پیامک، انتخاب متن آماده و مشاهده تاریخچه.
- [x] **۷.۴ تنظیمات باشگاه:** فرم و صفحه `settings.php` جهت تنظیم نام، آدرس، لوگو و کلیدهای API.

---

## 🟢 فاز ۸: آپلود، ورود/خروج داده و نسخه پشتیبان (Phase 8: Backup & Safe Sync) [تکمیل شد]
- [x] **۸.۱ ورود داده‌ها:** صفحه `import_export.php` جهت ورود لیست اعضا از اکسل و تصاویر از ZIP.
- [x] **۸.۲ نسخه پشتیبان:** خروجی مستقیم پشتیبان کامل دیتابیس به صورت فایل JSON.
